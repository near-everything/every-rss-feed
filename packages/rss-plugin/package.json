{
  "name": "@curatedotfun/rss-plugin",
  "version": "1.0.0",
  "description": "Unified RSS plugin with HOST and PROXY modes",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "peerDependencies": {
    "every-plugin": "^0.2.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@effect/language-service": "^0.41.1",
    "@module-federation/node": "^2.7.17",
    "@rspack/cli": "^1.5.2",
    "@rspack/core": "^1.5.2",
    "@types/ioredis": "^5.0.0",
    "@types/node": "^24.6.0",
    "dotenv": "^17.2.2",
    "every-plugin": "link:every-plugin",
    "msw": "^2.3.0",
    "sirv": "^3.0.2",
    "testcontainers": "^11.7.1",
    "vitest": "^3.2.4",
    "zephyr-rspack-plugin": "link:zephyr-rspack-plugin"
  },
  "scripts": {
    "build": "rspack build && tsc -p tsconfig.build.json",
    "type-check": "tsc -p tsconfig.build.json --noEmit",
    "dev": "rspack serve",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest --config vitest.integration.config.ts",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@orpc/client": "^1.9.3",
    "feed": "^5.1.0",
    "ioredis": "^5.8.1",
    "telegraf": "^4.16.3"
  }
}
